{{- if .Values.secret.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
  labels:
    app.kubernetes.io/instance: {{ .Release.Name }}
type: Opaque
data:
  SECRET_KEY: {{ .Values.secret.data.SECRET_KEY | b64enc }}
{{- end }}
